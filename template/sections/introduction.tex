\section{Introduction}
This is a reusable mathematical-paper template. See \cref{thm:sample}.

\begin{theorem}\label{thm:sample}
For every $x\in\mathbb{R}$, one has $x^2\ge 0$.
\end{theorem}

\begin{proof}
The claim follows from the order properties of $\mathbb{R}$.
\end{proof}
